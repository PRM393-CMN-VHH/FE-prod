# Copy this file to .env and fill in your own machine's LAN IP.
# .env is gitignored — each developer keeps their own, so this value never
# needs to be edited in source code again.
#
# Find your IP: `ipconfig getifaddr en0` (macOS) or `ipconfig` (Windows).
API_BASE_URL=http://192.168.1.10:3636
